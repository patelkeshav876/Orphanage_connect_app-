# MongoDB Atlas (required for API)
MONGODB_URI= // paste mongo uri here

# API port (default 4000)
PORT=4000

# Frontend: base URL for REST API (Vite dev proxies /api → localhost:4000)
VITE_API_URL=/api
